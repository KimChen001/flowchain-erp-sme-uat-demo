import { randomUUID } from 'node:crypto'
import { mkdir, readFile, rename, rm, writeFile } from 'node:fs/promises'
import { dirname } from 'node:path'

const now = () => new Date().toISOString()
const clone = value => structuredClone(value)
const text = value => String(value ?? '').trim()
const fail = (code, message, status = 400, details = []) => { const error = new Error(message); Object.assign(error, { code, status, details }); throw error }
export const emptySupplierRuntime = () => ({ schemaVersion: 1, initialized: true, updatedAt: now(), suppliers: [], itemSupplierRelationships: [], auditEvents: [] })

async function atomicWrite(file, document) { const temp=`${file}.tmp-${process.pid}-${Date.now()}`; await mkdir(dirname(file),{recursive:true}); try { await writeFile(temp,JSON.stringify(document,null,2),'utf8'); await rename(temp,file) } catch(error) { await rm(temp,{force:true}).catch(()=>{}); throw error } }
const sensitive = supplier => ({ ...supplier, bankAccountNumber: supplier.bankAccountNumber ? `****${supplier.bankAccountNumber.slice(-4)}` : '' })
const selector = (supplier, relationship) => ({ id:supplier.id, name:supplier.supplierName, supplierCode:supplier.supplierCode, supplierName:supplier.supplierName, status:supplier.status, categories:supplier.categories, preferred:Boolean(relationship?.preferred), defaultCurrency:supplier.defaultCurrency, paymentTermsId:supplier.paymentTermsId })

export function createDurableSupplierRepository({ dataFile }) {
  let document
  async function load() { if(document)return document; try { document=JSON.parse(await readFile(dataFile,'utf8')) } catch(error) { if(error.code!=='ENOENT')throw error; document=emptySupplierRuntime(); await atomicWrite(dataFile,document) } document={...emptySupplierRuntime(),...document,initialized:true}; return document }
  async function save() { document.updatedAt=now(); await atomicWrite(dataFile,document) }
  const findSupplier=(doc,key)=>doc.suppliers.find(row=>[row.id,row.supplierCode,row.supplierName].some(value=>text(value).toLowerCase()===text(key).toLowerCase()))
  const validate=(doc,input,existing=null)=>{
    const supplierCode=text(input.supplierCode ?? existing?.supplierCode), supplierName=text(input.supplierName ?? input.name ?? existing?.supplierName)
    if(!supplierCode)fail('SUPPLIER_CODE_REQUIRED','供应商编号必填',400,[{field:'supplierCode'}]); if(!supplierName)fail('SUPPLIER_NAME_REQUIRED','供应商名称必填',400,[{field:'supplierName'}])
    if(doc.suppliers.some(row=>row.id!==existing?.id&&row.supplierCode.toLowerCase()===supplierCode.toLowerCase()))fail('SUPPLIER_CODE_DUPLICATE','供应商编号已存在',409,[{field:'supplierCode'}])
    const email=text(input.email ?? existing?.email); if(email&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email))fail('SUPPLIER_EMAIL_INVALID','邮箱格式不正确',400,[{field:'email'}])
    const delivery=Number(input.deliveryCycleDays ?? existing?.deliveryCycleDays ?? 0); if(delivery<0)fail('DELIVERY_CYCLE_INVALID','送货周期必须大于 0',400,[{field:'deliveryCycleDays'}])
    for(const [field,code] of [['creditCode','CREDIT_CODE_DUPLICATE'],['taxIdentificationNumber','TAX_ID_DUPLICATE']]) { const value=text(input[field] ?? existing?.[field]); if(value&&doc.suppliers.some(row=>row.id!==existing?.id&&text(row[field]).toLowerCase()===value.toLowerCase()))fail(code,`${field} 已存在`,409,[{field}]) }
    const status=text(input.status ?? existing?.status ?? 'draft'); if(!['draft','active','inactive'].includes(status))fail('SUPPLIER_STATUS_INVALID','供应商状态无效')
    return { supplierCode,supplierName,email,deliveryCycleDays:delivery,status }
  }
  return {
    mode:'json', adapter:'durable-supplier-master-v1', _dataFile:dataFile,
    async listSuppliers(filters={}) { const doc=await load(); const q=text(filters.query).toLowerCase(); return clone(doc.suppliers.filter(row=>(!filters.status||row.status===filters.status)&&(!filters.category||row.categories.includes(filters.category))&&(!q||[row.supplierCode,row.supplierName,row.contactName].some(v=>text(v).toLowerCase().includes(q)))).map(sensitive)) },
    async getSupplier(key,{privileged=false}={}) { const row=findSupplier(await load(),decodeURIComponent(key)); return row?clone(privileged?row:sensitive(row)):null },
    async createSupplier(input,actor) { const doc=await load(), valid=validate(doc,input); const t=now(); const row={ id:`SUP-${randomUUID().slice(0,8).toUpperCase()}`, supplierCode:valid.supplierCode, supplierName:valid.supplierName, shortName:text(input.shortName), mnemonicCode:text(input.mnemonicCode), status:valid.status, businessType:text(input.businessType), categories:Array.isArray(input.categories)?input.categories.map(text).filter(Boolean):[], contactName:text(input.contactName), telephone:text(input.telephone), email:valid.email, fax:text(input.fax), country:text(input.country), regionId:text(input.regionId), cityId:text(input.cityId), postalCode:text(input.postalCode), address:text(input.address), deliveryCycleDays:valid.deliveryCycleDays, defaultCurrency:text(input.defaultCurrency)||'CNY', paymentTermsId:text(input.paymentTermsId)||'NET30', settlementMethod:text(input.settlementMethod), creditCode:text(input.creditCode), taxIdentificationNumber:text(input.taxIdentificationNumber), bankName:text(input.bankName), bankAccountName:text(input.bankAccountName), bankAccountNumber:text(input.bankAccountNumber), internalComment:text(input.internalComment), version:1, createdBy:actor, createdAt:t, updatedBy:actor, updatedAt:t }; doc.suppliers.push(row); doc.auditEvents.push({id:`AUD-${randomUUID()}`,action:'created',supplierId:row.id,actor,timestamp:t,version:1}); await save(); return clone(sensitive(row)) },
    async updateSupplier(key,input,actor) { const doc=await load(), row=findSupplier(doc,decodeURIComponent(key)); if(!row)fail('SUPPLIER_NOT_FOUND','供应商不存在',404); if(Number(input.expectedVersion)!==row.version)fail('VERSION_CONFLICT','供应商已被其他用户更新',409); const valid=validate(doc,input,row), before={...row}; const fields=['shortName','mnemonicCode','businessType','categories','contactName','telephone','fax','country','regionId','cityId','postalCode','address','defaultCurrency','paymentTermsId','settlementMethod','creditCode','taxIdentificationNumber','bankName','bankAccountName','bankAccountNumber','internalComment']; Object.assign(row,{supplierCode:valid.supplierCode,supplierName:valid.supplierName,email:valid.email,deliveryCycleDays:valid.deliveryCycleDays,status:valid.status}); for(const field of fields)if(input[field]!==undefined)row[field]=Array.isArray(input[field])?input[field].map(text).filter(Boolean):text(input[field]); row.version++; row.updatedBy=actor; row.updatedAt=now(); doc.auditEvents.push({id:`AUD-${randomUUID()}`,action:before.status!==row.status?(row.status==='active'?'activated':'deactivated'):'updated',supplierId:row.id,actor,timestamp:row.updatedAt,version:row.version,bankDetailsChanged:before.bankAccountNumber!==row.bankAccountNumber}); await save(); return clone(sensitive(row)) },
    async selectSuppliers(filters={}) { const doc=await load(); const q=text(filters.query).toLowerCase(); return clone(doc.suppliers.filter(row=>row.status==='active'&&(!q||[row.supplierCode,row.supplierName].some(v=>v.toLowerCase().includes(q)))).map(row=>selector(row))) },
    async listItemSuppliers(itemId) { const doc=await load(); return clone(doc.itemSupplierRelationships.filter(r=>r.itemId===itemId).map(r=>({...r,supplier:selector(findSupplier(doc,r.supplierId)||{},r)}))) },
    async listSupplierItems(supplierId) { return clone((await load()).itemSupplierRelationships.filter(r=>r.supplierId===supplierId)) },
    async createItemSupplier(itemId,input,actor,item) { const doc=await load(), supplier=findSupplier(doc,input.supplierId); if(!supplier)fail('SUPPLIER_NOT_FOUND','供应商不存在',404); if(input.active!==false&&supplier.status!=='active')fail('SUPPLIER_INACTIVE','停用供应商不能建立启用关系'); if(!item||item.status!=='active')fail('ITEM_INACTIVE','停用或不存在的 SKU 不能建立启用关系'); if(input.preferred)doc.itemSupplierRelationships.filter(r=>r.itemId===itemId).forEach(r=>r.preferred=false); const t=now(), row={relationshipId:`ISR-${randomUUID().slice(0,8).toUpperCase()}`,itemId,supplierId:supplier.id,supplierSku:text(input.supplierSku),active:input.active!==false,approved:input.approved!==false,preferred:Boolean(input.preferred),priority:Number(input.priority||0),purchaseUnit:text(input.purchaseUnit)||item.purchaseUnit||item.baseUnit,referencePrice:Number(input.referencePrice||0),currency:text(input.currency)||supplier.defaultCurrency,leadTimeDays:Number(input.leadTimeDays||0),minimumOrderQuantity:Number(input.minimumOrderQuantity||1),contractId:text(input.contractId),version:1,createdBy:actor,createdAt:t,updatedBy:actor,updatedAt:t}; doc.itemSupplierRelationships.push(row); doc.auditEvents.push({id:`AUD-${randomUUID()}`,action:'item_relationship_added',supplierId:supplier.id,itemId,relationshipId:row.relationshipId,actor,timestamp:t}); await save(); return clone(row) },
    async updateItemSupplier(itemId,relationshipId,input,actor) { const doc=await load(), row=doc.itemSupplierRelationships.find(r=>r.itemId===itemId&&r.relationshipId===relationshipId); if(!row)fail('RELATIONSHIP_NOT_FOUND','SKU–供应商关系不存在',404); if(Number(input.expectedVersion)!==row.version)fail('VERSION_CONFLICT','关系已被其他用户更新',409); if(input.preferred)doc.itemSupplierRelationships.filter(r=>r.itemId===itemId&&r.relationshipId!==relationshipId).forEach(r=>r.preferred=false); for(const field of ['supplierSku','active','approved','preferred','priority','purchaseUnit','referencePrice','currency','leadTimeDays','minimumOrderQuantity','contractId'])if(input[field]!==undefined)row[field]=input[field]; row.version++;row.updatedBy=actor;row.updatedAt=now();doc.auditEvents.push({id:`AUD-${randomUUID()}`,action:'item_relationship_updated',supplierId:row.supplierId,itemId,relationshipId,actor,timestamp:row.updatedAt});await save();return clone(row) },
    async approvedSuppliersForItem(itemId) { const doc=await load(); return clone(doc.itemSupplierRelationships.filter(r=>r.itemId===itemId&&r.active&&r.approved).map(r=>{const s=findSupplier(doc,r.supplierId);return s?.status==='active'?{...selector(s,r),relationshipId:r.relationshipId,referencePrice:r.referencePrice,currency:r.currency,leadTimeDays:r.leadTimeDays,minimumOrderQuantity:r.minimumOrderQuantity}:null}).filter(Boolean)) },
    async snapshot(){return clone(await load())},
  }
}
