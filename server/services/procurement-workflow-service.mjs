import { randomUUID } from 'node:crypto'
import { PR_TRANSITIONS, PO_TRANSITIONS, assertVersion, procurementError, recommendProcurementPath, transition, validateDirectPo } from '../domain/procurement-workflow.mjs'
const now=()=>new Date().toISOString(); const id=(prefix)=>`${prefix}-${randomUUID().slice(0,8).toUpperCase()}`
export function createProcurementWorkflowService({ repository, policyProvider=async()=>({}), permission=async()=>true }) {
  const audit=(doc,entry)=>{ const record={ id:id('AUD'), timestamp:now(), ...entry }; doc.auditEntries.push(record); return record.id }
  return {
    async createPurchaseRequest(input,actor){ return repository.transact(doc=>{ const t=now(); const pr={ id:input.id||id('PR'), workspaceId:input.workspaceId||'default', companyId:input.companyId||'default', status:'draft', procurementPath:'undecided', procurementPathDecision:null, requesterId:input.requesterId||actor, buyerId:input.buyerId||'', departmentId:input.departmentId||'', supplierId:input.supplierId||'', currency:input.currency||'CNY', paymentTermsId:input.paymentTermsId||'', expectedDeliveryDate:input.expectedDeliveryDate||'', totalAmount:Number(input.totalAmount||0), lines:input.lines||[], version:1, createdAt:t, createdBy:actor, updatedAt:t, updatedBy:actor, auditTrailIds:[] }; pr.auditTrailIds.push(audit(doc,{actor,entityType:'purchase_request',entityId:pr.id,action:'PR_CREATED',after:pr,result:'success'})); doc.purchaseRequests.push(pr); return pr }) },
    async transitionPurchaseRequest(prId,next,{expectedVersion,actor,reason=''}){ return repository.transact(doc=>{ const pr=doc.purchaseRequests.find(x=>x.id===prId); if(!pr) throw procurementError('ENTITY_NOT_FOUND','采购申请不存在',[],404); const event=transition(pr,next,PR_TRANSITIONS,expectedVersion,actor,`PR_${next.toUpperCase()}`); pr.auditTrailIds.push(audit(doc,{actor,entityType:'purchase_request',entityId:pr.id,reason,...event,result:'success'})); return pr }) },
    async recommendPath(prId,actor){ const pr=await repository.get('pr',prId); if(!pr) throw procurementError('ENTITY_NOT_FOUND','采购申请不存在',[],404); const result=recommendProcurementPath(pr,await policyProvider(pr),{},{}); await repository.transact(doc=>{ audit(doc,{actor,entityType:'purchase_request',entityId:prId,action:'PROCUREMENT_PATH_RECOMMENDED',after:result,result:'success'}); return result }); return result },
    async recordPathDecision(prId,input,actor){ return repository.transact(async doc=>{ const pr=doc.purchaseRequests.find(x=>x.id===prId); if(!pr) throw procurementError('ENTITY_NOT_FOUND','采购申请不存在',[],404); assertVersion(pr,input.expectedVersion); if(await repository.findLinkedPo(prId) || await repository.findLinkedRfq(prId)) throw procurementError('DOWNSTREAM_OBJECT_ALREADY_EXISTS','已存在下游采购对象',[],409); const rec=recommendProcurementPath(pr,await policyProvider(pr),{},{}); if(input.path==='direct_po' && rec.recommendation==='rfq' && !input.overrideReason) throw procurementError('RFQ_REQUIRED_BY_POLICY','选择直接采购需要填写覆盖原因'); pr.procurementPath=input.path; pr.procurementPathDecision={ path:input.path, selectedBy:actor, selectedAt:now(), reason:input.reason||'', recommendation:rec.recommendation, recommendationReasons:rec.recommendationReasons, policySnapshot:rec.policySnapshot, overrideReason:input.overrideReason||'' }; pr.version++; pr.updatedAt=now(); pr.updatedBy=actor; pr.auditTrailIds.push(audit(doc,{actor,entityType:'purchase_request',entityId:pr.id,action:input.overrideReason?'PROCUREMENT_PATH_OVERRIDDEN':'PROCUREMENT_PATH_SELECTED',after:pr.procurementPathDecision,policySnapshot:rec.policySnapshot,result:'success'})); return pr }) },
    validateDirectPo,
    transitionPurchaseOrder: async(poId,next,args)=>repository.transact(doc=>{ const po=doc.purchaseOrders.find(x=>x.id===poId); if(!po) throw procurementError('ENTITY_NOT_FOUND','采购订单不存在',[],404); const event=transition(po,next,PO_TRANSITIONS,args.expectedVersion,args.actor,`PO_${next.toUpperCase()}`); po.auditTrailIds.push(audit(doc,{actor:args.actor,entityType:'purchase_order',entityId:po.id,...event,result:'success'})); return po }),
  }
}
